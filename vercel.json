
{
    "version": 2,
    "builds": [
      { "src": "package.json", "use": "@vercel/node" },
    { "src": "package.json", "use": "@vercel/static-build" }
    ],
    "routes": [
      {
        "src": "/(.*)", 
        "dest": "server.js" 
      }
    ]
}
